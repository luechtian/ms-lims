---
feature: 001-parties-master-data
started: 2026-04-17
---

## Completed this session

- T001 — 2026-04-17 — user-written
- T002 — 2026-04-17 — user-written
- T003 — 2026-04-17 — user-written
- T004 — 2026-04-17 — user-written
- T005 — 2026-04-18 — user-written
- T006 — 2026-04-18 — user-written
- T007 — 2026-04-18 — user-written
- T008 — 2026-04-18 — user-written
- T009 — 2026-04-18 — user-written (commands only, Django generated the migration)

## Concepts covered

- Django `AppConfig` (name, default_auto_field, verbose_name) — T001 — 2026-04-17 — needs revisit (user's mental model for `name` is "namespace/import clarity"; correct job is "which package holds the app's models/migrations/fixtures/templates/static"; will land concretely at T009 `makemigrations`)
- `INSTALLED_APPS` short-form vs explicit AppConfig path + `default = True` — T002 — 2026-04-17 — needs revisit (user thought short form = app doesn't load at all; correct: app loads but with auto-generated bare AppConfig, silently dropping custom attrs + `ready()` hook; will revisit when an app first needs a `ready()` hook)
- `pyproject.toml`: `[project].dependencies` vs `[dependency-groups]` (PEP 735) vs `[project.optional-dependencies]` — T003 — 2026-04-17
- `uv add` / `uv sync` basics + `UV_LINK_MODE` hardlink warning (cache on C:\, venv on D:\) — T003 — 2026-04-17
- `__init__.py` as package marker + `conftest.py` hierarchical fixture scope — T004 — 2026-04-17
- `DjangoModelFactory` + `SubFactory` + `factory.Sequence` — T011 — 2026-04-18. Sequence-Grund (UniqueConstraint) beim Comprehension-Check nicht gewusst — nacherklärt. Lambda-Syntax auch nicht bekannt — nacherklärt. Needs revisit: Sequence-Regel (Unique→Sequence, sonst Faker) bei T012 nochmal prüfen.
- Phase-2-Recap Konsolidierungsfragen: on_delete=PROTECT (richtig), circular SubFactory loop (nachgeschaut im Chatverlauf — Konzept verstanden aber nicht selbst abgerufen — needs revisit in Phase 3), Meta.ordering (richtig).
- Django ORM-Grundlagen (Model ↔ Tabelle, `objects` Default-Manager, QuerySet als lazy chainable result) — T005 — 2026-04-17 (foundational briefing nachgeliefert nach user-Rückmeldung "ich weiß noch nichts")
- Manager-Subclass vs. QuerySet-Subclass + `as_manager()` (warum custom Methoden auf QuerySet, nicht auf Manager — chaining nach `.filter()` geht nur mit QuerySet-Variante; Manager ist eine Factory mit `get_queryset()`) — T005 — 2026-04-17 — needs revisit (user sagt "ein wenig verstanden"; wird konkret bei T006 `as_manager()`-Zuweisung, T025 Tests, T039 Admin-FK-Filter)
- `.active()` vs `filter(active=True)`: Single Source of Truth, grep-bar, Domain-DSL — nicht nur Lesbarkeit — T005 — 2026-04-17
- ORM-Landkarte (Model↔Table, Instance↔Row, Manager↔Tablewide-Access, QuerySet↔lazy SQL, Migration↔Schema-Sync) — 3-Zeilen-Orientierung vor T006 — 2026-04-18 — volle Tiefe schrittweise über T006 (Fields/Meta) → T008 (Instance-Lifecycle) → T009 (Migration-Mechanik)
- `db_index=True` — DB-Level-B-Tree-Index, beschleunigt Filters/Joins/Sorts auf der Spalte; kostet Schreib-Performance + Disk. Regel: setzen bei häufiger Query-Nutzung (WHERE/ORDER BY/Join), sonst weg. Partial-Index geht mit Django's db_index-Flag nicht — für Minorität-Queries eher `Meta.indexes` mit condition. — T006-Nachfrage — 2026-04-18
- Model-Klasse + Field-Typen (CharField mit max_length, TextField, URLField, BooleanField; blank= vs null=; default=""-Konvention für String-Felder mit blank=True) — T006 — 2026-04-18
- `class Meta` (inner class als Tabellen-Metadaten-Container — ordering, constraints, indexes, app_label, etc.; nicht für Spalten) — T006 — 2026-04-18
- `UniqueConstraint(Lower("name"), name="uniq_..._ci")` als case-insensitive functional unique index — T006 — 2026-04-18. User-Comprehension in T006-Review: "2 unabhängige DB-Einträge, die das gleiche meinen" — Kern erfasst, Nuance (Lower normalisiert Strings VOR der Eindeutigkeitsprüfung) ergänzt.
- `__str__`-Override (für Admin-Dropdowns, print, repr-Fallbacks; N+1-Risiko wenn Query-triggert) — T006 — 2026-04-18
- `ForeignKey` + `on_delete` (PROTECT vs SET_NULL vs CASCADE als Geschäftsregel "was passiert, wenn Parent gelöscht wird"; null=True+blank=True Kombo für optionale FKs) — T007 — 2026-04-18
- `related_name` + `"+"`-Pattern (Default `<model>_set`, sprechendere Namen wie "research_groups" / "persons"; `"+"` = kein Reverse-Accessor, wenn Relation 0..1 oder ungenutzt) — T007 — 2026-04-18. Comprehension-Frage (led_groups vs "+") erst nicht beantwortbar — mit konkreter Alice-Szene re-teached; jetzt verstanden.
- String-FK-Reference (`ForeignKey("Person", ...)` für Lazy Resolution bei später definierten Models; `manage.py check` bleibt zwischen T007 und T008 rot, normal) — T007 — 2026-04-18
- Partial `UniqueConstraint` + `Q`-Objects (`condition=Q(...)` als partial index; zwei Constraints nötig wegen SQL-NULL-Distinktheit) — T007 — 2026-04-18
- `auto_now_add` (once on INSERT) vs `auto_now` (every save, NICHT bei QuerySet.update(...)) — T007 — 2026-04-18
- `active_members()` Reverse-Accessor-Chain (`self.persons` als RelatedManager → `.active()` drauf aufrufen) — T007 — 2026-04-18. Anfangs nicht verstanden, weil im Briefing nicht als eigenes Konzept erklärt — mit Alice/Bob/Carol-Szene step-by-step nachgeteacht und verstanden.
- `Meta.indexes` (composite B-Tree-Index über mehrere Spalten in fester Reihenfolge; Präfix-Matching — `["last_name","first_name"]` hilft auch Queries, die nur nach `last_name` filtern; Abgrenzung zu `db_index=True` = single-column-am-Field) — T008 — 2026-04-18
- `@property` Python-Decorator (macht aus einer Methode einen Attribut-Zugriff ohne Klammern; reines Python, keine DB-Spalte, nicht filterbar/sortierbar) — T008 — 2026-04-18
- `EmailField` (CharField mit eingebautem Email-Format-Validator bei `full_clean()`; default `max_length=254` RFC; in diesem Projekt bewusst **kein** `unique=True` — advisory only per data-model.md) — T008 — 2026-04-18
- `__str__` vs `@property`-Name-Accessor — verschiedene Jobs: `__str__` ist Django-Hook (Admin-Dropdowns, `print`, `{{ person }}` im Template, ModelChoiceField-Labels), `full_name` ist explizite API für Code/Templates/Serializer/Tests. Heute gleicher String, morgen darf `__str__` sich ändern (z.B. `"Mueller, Alice"`) ohne den Code-API-Pfad zu brechen. Comprehension-Frage T008 — initial unklar, mit Szene ("Chef will im Admin Nachname zuerst") geklärt. — T008 — 2026-04-18
- Django-Migrations als versionierte Schema-Historie: `makemigrations` liest models.py + vergleicht mit Migration-Chain + schreibt neues `000N_<name>.py`-File; `migrate` prüft `django_migrations`-Tabelle und wendet fehlende an. `dependencies=[...]` bildet den DAG. Constraints + Indexes werden automatisch als Operations übersetzt. Regel: jede Model-Änderung + generiertes Migration-File im selben Commit. — T009 — 2026-04-18
- Zirkuläre-FK-Auflösung auf zwei Zeitlinien: (1) Python-load-time — String-Reference `"Person"` vermeidet `NameError` auf forward references; (2) Migration-Generation — Django bricht den FK-Zyklus durch separaten `AddField`-Step nach `CreateModel` auf. String vs Klassen-Reference beeinflusst nur (1), Migration-File sieht in beiden Fällen identisch aus. Comprehension-Frage T009 — user wusste es nicht, mit Alice-analoger Erklärung (zwei Zeitlinien sauber getrennt) nachgeteacht. — T009 — 2026-04-18
- `admin_client` fixture (pytest-django built-in: creates superuser + logged-in Client) — T016 — 2026-04-19
- Admin-POST-Pattern (URL schema /admin/<app>/<model>/add/ and /change/<pk>/; 302=success, 200=validation error; form data = model field names + "_save": "Save") — T016 — 2026-04-19
- `refresh_from_db()` (reloads in-memory instance from DB after external write) — T016 — 2026-04-19
- Model.clean() als cross-field Validation-Hook (full_clean()→clean()→admin Feldenfehler; save() ruft clean() NICHT auf; super().clean() immer zuerst; ValidationError als Dict für Feldenfehler) — T017 — 2026-04-19

- T010 — 2026-04-18 — user-written
- T011 — 2026-04-18 — user-written
- T012 — 2026-04-18 — user-written
- T013 — 2026-04-19 — user-written
- T014 — 2026-04-19 — user-written
- T015 — 2026-04-19 — user-written
- T016 — 2026-04-19 — user-written
- T017 — 2026-04-19 — user-written
- T018 — 2026-04-19 — user-written

## In-progress state (falls Session unterbrochen wird)

T018 abgeschlossen. Phase 3 (US1) vollständig. Nächster Task: T019 — Phase 4 (US2) beginnt mit test_internal_lab_fixture_is_valid_json in tests/parties/test_seed.py.

## Review notes

- T001 briefing shipped an incomplete `PartiesConfig` snippet (missing import + `name`). Standing rule saved to global memory: briefing code blocks must be runnable or marked `# excerpt`.
- User ran `startapp` (option 1) — left `views.py` / `tests.py` and placeholder comments behind; cleaned up on review. Watch for same pattern if they `startapp` other apps later.
- T003 tasks.md says `uv sync --extra dev` but this project uses `[dependency-groups]` (PEP 735), so the correct command is `uv sync` or `uv sync --group dev`. `--extra` maps to `[project.optional-dependencies]`. Flag if the same drift appears in later tasks.
- User's Windows environment: `.venv` on D:\, uv cache on C:\, so uv emits hardlink-fallback warning. Harmless; `$env:UV_LINK_MODE = "copy"` silences it.
- **2026-04-18**: User hat ORM-Verständnis als "nur angeschnitten" eingestuft und um vollen Build-out gebeten. Gemeinsam entschieden: **learning by doing, schrittweise** statt monolithischer Deep-Dive — ORM-Konzepte laden sich durch T006–T009 schrittweise (Model/Fields → Instance → Manager-Zuweisung → Migration), 3-Zeilen-Landkarte vorab zur Orientierung.
- **2026-04-18**: Briefing-Format umgestellt auf zwei Tiers (Teaching-Tier mit 7 Sub-Blöcken + Umfang-Header; Apply-Tier knapp) — siehe globales Memory `feedback_briefing_format`. Grund: User will klare Struktur + Zeitschätzung oben, damit er vor dem Tippen "zu viel, kürzer" sagen kann.
- **2026-04-18 — Teaching-Stil innerhalb der Konzept-Blöcke angepasst:** User-Feedback bei T007: `*Mental Model:*` / `*Fallstrick:*`-Labels funktionierten nicht; Konzepte wurden mit Jargon-Ketten in einem Absatz eingeführt, ohne dass konkrete Beispiele vorangingen. **Neue Regel innerhalb der Konzept-Blöcke:** konkrete Szene zuerst (named examples wie "Alice, Bob, Carol in max_planck"), ein Konzept pro Satz, Step-by-step-Execution-Trace, Fließtext statt Labels. Bewährt bei Re-teach von `active_members()` und `related_name="+"`. Gilt ab T008 und allen folgenden. Skill-Template (`task-briefing.md`) behält die Sektionen, aber der Konzept-*Inhalt* folgt ab jetzt dem neuen Stil — ein späteres Template-Update, das die Label-Struktur rausnimmt, ist optional.
